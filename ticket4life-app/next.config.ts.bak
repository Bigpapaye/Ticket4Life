import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Temporary: ignore ESLint errors during build while refactors are ongoing.
  eslint: {
    ignoreDuringBuilds: true,
  },
};

export default nextConfig;
